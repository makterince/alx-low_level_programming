#include <stdio.h>
#include "main.h"
/**
 *reverse_array - reverse the content of an array
 *@a: parameter
 *@n: parameter
 *
 */
void reverse_array(int *a, int n)
{
	int i,

	for (i = n - 1; a[i] >= 0; i--)
	{
		;
	}
}
