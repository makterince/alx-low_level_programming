#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: parameter
 * Return: value of length
 */
int _strlen(char *s)
{
	int len = 0;

	while
	{
		(str[len] != '\0')
			len++
	}
	return len
}
