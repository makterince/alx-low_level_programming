#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: parameter
 * Return: value of length
 */
int _strlen(char *s)
{
	int len;

	while (s[len] != '\0')
	{
		return (len);
		len++;
	}
}
