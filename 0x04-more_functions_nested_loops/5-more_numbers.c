#include "main.h"
#include <stdio.h>
/**
 *more_numbers - prints 1 to 14 in 10 rows
 *
 *
 */
void more_numbers(void)
{
	int a, b;

	for (b = 48; b <= 57; b++)
	{
		for (a = 48; a <= 63; a++)
		{
			if (a > 57)
			{
			_putchar(48);

			if (c == 58)
				c = 48;
			}
		_putchar(c);
		c++;
		}
	_putchar('\n');
	}
}
